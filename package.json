{
  "name": "luke-the-moonshot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "verify": "python verify.py && npm test && node --check script.js && node --check link-config.js && node --check runtime-config.js",
    "build": "node scripts/build.mjs",
    "check": "npm run verify && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
